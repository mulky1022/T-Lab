[PHP]
; Keep this configuration portable for local development and cloud deployments.
; Do not hard-code Windows extension paths here because Railway/Linux builds
; load extensions from the runtime's default extension directory instead.
; If you need to enable extensions explicitly, use the platform-native names
; such as "pdo_pgsql" or "pgsql" on Linux and "php_pdo_pgsql.dll" on Windows.

; curl.cainfo = "C:/Program Files/Git/usr/ssl/certs/ca-bundle.crt"
; openssl.cafile = "C:/Program Files/Git/usr/ssl/certs/ca-bundle.crt"
